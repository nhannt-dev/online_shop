{
    "private": true,
    "type": "module",
    "scripts": {
        "start": "start http://127.0.0.1:8000/admin/login && php artisan serve",
        "controller": "php artisan make:controller admin/HomeController",
        "mgrt": "php artisan migrate",
        "tinker": "php artisan tinker",
        "sleep5s": "node -e \"setTimeout(() => process.exit(0), 5000)\"",
        "docker": "docker compose down && docker compose up -d && yarn sleep5s && yarn mgrt",
        "tbl": "php artisan make:migration alter_users_table",
        "dev": "vite",
        "build": "vite build"
    },
    "devDependencies": {
        "axios": "^1.6.1",
        "laravel-vite-plugin": "^0.8.0",
        "vite": "^4.0.0"
    }
}
